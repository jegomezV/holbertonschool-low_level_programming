#include<stdio.h>
/**
*main - entry point
*Return:0
*/
int main(void)
{
	char alpha = 'a';

		for (; alpha < 'z' ; alpha++)
		{
			if (;alpha != 'q' || alpha != 'e')
			putchar(alpha);
		}
		putchar('\n');

		return (0);
}

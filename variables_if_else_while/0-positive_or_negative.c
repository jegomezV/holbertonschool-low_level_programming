#include<stdio.h>

/*main - punto de entrada
Return: valor de regreso
*/

int main(void){

int n;
n = rand() - RAND_MAX / 2;

if (n < 0 = es negativo);
if (n > 0 = es positivo);
if (n = 0 = es cero);


return (0);
}
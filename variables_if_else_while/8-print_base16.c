#include<stdio.h>

/**
*main - entry point
*Return: 0
*/
int main(void)
{
	char let = 'a';
	char word = '0';

		for (word = '0'; word <= '9' ; word++)
			putchar(word);

		for (let = 'a'; a <= 'f' ; let++)
			putchar(let);

			putchar('\n');
return (0);
}

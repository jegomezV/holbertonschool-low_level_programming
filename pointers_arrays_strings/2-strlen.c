#include "main.h"
/**
 * _strlen - sting long
 * @s: var
 * Return: 0
*/

int _strlen(char *s)
{
	char *s;
	int tring;

	s = "My first strlen!";
    tring = strlen(s);
    printf("%d\n", tring);

	return (0);
}

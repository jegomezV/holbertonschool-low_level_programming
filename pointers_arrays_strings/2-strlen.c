#include "main.h"
#include <string.h>
/**
 * _strlen - sting long
 * @s: var
 * Return: 0
 */

int _strlen(char *s)
{
	char *s;
	int longitud = _strlen(s);

	printf("La longitud de la cadena '%s' es: %d", s, longitud);
	return 0;
}
